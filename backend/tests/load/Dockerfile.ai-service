# Mock AI Service Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装依赖
RUN pip install fastapi uvicorn

# 创建mock AI服务
COPY mock_ai_service.py .

# 暴露端口
EXPOSE 8001

# 启动命令
CMD ["uvicorn", "mock_ai_service:app", "--host", "0.0.0.0", "--port", "8001"]
